name: createTag
on:
  push:
    branches:
      - main
      - engineering
#     tags:
#       - 'release' # question: add certain conditions in here? 

env: 
  version: EngBuild12 # question where to get version from? 
  branch_merge: ${{ github.ref_name }}
  branch_push: ${{ github.event.pusher.name }}
  head_ref: $GITHUB_HEAD_REF
  base_ref: $GITHUB_BASE_REF
  path: $GITHUB_EVENT_PATH
  
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: env
#   create-tag:
#     runs-on: ubuntu-latest
#     steps:   
#     - name: Create tag
#       uses: actions/github-script@v5
#       with:
#         script: |
#           github.rest.git.createRef({
#             owner: context.repo.owner,
#             repo: context.repo.repo,
#             ref: 'refs/tags/${{ env.version }}',
#             sha: context.sha
#             })
#   run-workflow:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Run other workflow
#       run: |
#         echo "Run workflow"
#         sleep 10s
#     - uses: convictional/trigger-workflow-and-wait@v1.6.1
#       with:
#         owner: lhofer12
#         repo: test_docs_target
#         github_token: ${{ secrets.G_ACCESS_TOKEN }}
#         workflow_file_name: Testing.yml
#         client_payload: '{"version": "${{ env.version }}"}'
# #         version: "${{ env.version }}"
# #         client_payload: '{"inputs":{"version": "${{ env.version }}" }}'

